import { ripemd160 } from '@noble/hashes/legacy.js';
import { hex } from '@scure/base';
import { opcodes, script } from 'liquidjs-lib';
import { reverseBuffer } from 'liquidjs-lib/src/bufferutils.js';
import bitcoinReverseSwapTree from "../../swap/ReverseSwapTree.js";
import { TAP_LEAF_VERSION_LIQUID } from "../../swap/TaprootUtils.js";
import { assignTreeProbabilities, sortTree } from "../../swap/TreeSort.js";
import { getScriptIntrospectionValues } from "../Utils.js";
var Feature;
(function (Feature) {
    Feature[Feature["ClaimCovenant"] = 0] = "ClaimCovenant";
})(Feature || (Feature = {}));
const claimCovenantOutputIndex = script.number.encode(0);
const createClaimCovenantLeaf = (preimageHash, assetHash, outputScript, expectedAmount) => {
    const userOutput = getScriptIntrospectionValues(outputScript);
    const amountBuffer = Buffer.alloc(8);
    amountBuffer.writeBigUint64LE(BigInt(expectedAmount));
    return {
        version: TAP_LEAF_VERSION_LIQUID,
        output: script.compile([
            opcodes.OP_SIZE,
            script.number.encode(32),
            opcodes.OP_EQUALVERIFY,
            opcodes.OP_HASH160,
            Buffer.from(ripemd160(preimageHash)),
            opcodes.OP_EQUALVERIFY,
            claimCovenantOutputIndex,
            opcodes.OP_INSPECTOUTPUTSCRIPTPUBKEY,
            script.number.encode(userOutput.version),
            opcodes.OP_EQUALVERIFY,
            userOutput.script,
            opcodes.OP_EQUALVERIFY,
            claimCovenantOutputIndex,
            opcodes.OP_INSPECTOUTPUTASSET,
            opcodes.OP_1,
            opcodes.OP_EQUALVERIFY,
            reverseBuffer(Buffer.from(hex.decode(assetHash))),
            opcodes.OP_EQUALVERIFY,
            claimCovenantOutputIndex,
            opcodes.OP_INSPECTOUTPUTVALUE,
            opcodes.OP_DROP,
            amountBuffer,
            opcodes.OP_EQUAL,
        ]),
    };
};
const reverseSwapTree = (preimageHash, claimPublicKey, refundPublicKey, timeoutBlockHeight, features) => {
    const tree = bitcoinReverseSwapTree(true, preimageHash, claimPublicKey, refundPublicKey, timeoutBlockHeight);
    if (features === undefined) {
        return tree;
    }
    if (new Set(features.map((feature) => feature.type)).size !== features.length) {
        throw new Error('duplicate feature');
    }
    for (const feature of features) {
        switch (feature.type) {
            case Feature.ClaimCovenant:
                tree.covenantClaimLeaf = createClaimCovenantLeaf(preimageHash, feature.assetHash, feature.outputScript, feature.expectedAmount);
                break;
            default:
                throw new Error(`unknown feature: ${feature.type}`);
        }
    }
    return {
        ...tree,
        tree: sortTree(assignTreeProbabilities(tree)),
    };
};
export default reverseSwapTree;
export { Feature };
//# sourceMappingURL=ReverseSwapTree.js.map