import { ripemd160 } from '@noble/hashes/legacy.js';
import { Script } from '@scure/btc-signer';
import { createLeaf, swapLeafsToTree, toXOnly } from "./TaprootUtils.js";
export const createRefundLeaf = (isLiquid, refundPublicKey, timeoutBlockHeight) => createLeaf(isLiquid, [
    toXOnly(refundPublicKey),
    'CHECKSIGVERIFY',
    timeoutBlockHeight,
    'CHECKLOCKTIMEVERIFY',
]);
export const extractClaimPublicKeyFromSwapTree = (swapTree) => {
    const pubkey = Script.decode(swapTree.claimLeaf.output)?.[3];
    if (pubkey === undefined || !(pubkey instanceof Uint8Array)) {
        throw new Error('invalid claim public key');
    }
    return pubkey;
};
export const extractRefundPublicKeyFromSwapTree = (swapTree) => {
    const pubkey = Script.decode(swapTree.refundLeaf.output)?.[0];
    if (pubkey === undefined || !(pubkey instanceof Uint8Array)) {
        throw new Error('invalid refund public key');
    }
    return pubkey;
};
const swapTree = (isLiquid, preimageHash, claimPublicKey, refundPublicKey, timeoutBlockHeight) => {
    const claimLeaf = createLeaf(isLiquid, [
        'HASH160',
        ripemd160(preimageHash),
        'EQUALVERIFY',
        toXOnly(claimPublicKey),
        'CHECKSIG',
    ]);
    const refundLeaf = createRefundLeaf(isLiquid, refundPublicKey, timeoutBlockHeight);
    return {
        claimLeaf,
        refundLeaf,
        tree: swapLeafsToTree(claimLeaf, refundLeaf),
    };
};
export const fundingAddressTree = (isLiquid, refundPublicKey, timeoutBlockHeight) => {
    const refundLeaf = createRefundLeaf(isLiquid, refundPublicKey, timeoutBlockHeight);
    return {
        refundLeaf,
        tree: refundLeaf,
    };
};
export default swapTree;
//# sourceMappingURL=SwapTree.js.map