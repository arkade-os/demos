import { equalBytes } from '@scure/btc-signer/utils.js';
const compareLeaf = (leaf, compareLeaf) => leaf.version === compareLeaf.version &&
    equalBytes(leaf.output, compareLeaf.output);
const compareTree = (tree, compare) => {
    if (Array.isArray(tree) !== Array.isArray(compare)) {
        return false;
    }
    if (Array.isArray(tree) && Array.isArray(compare)) {
        return (tree.length === compare.length &&
            tree.every((leaf, i) => compareTree(leaf, compare[i])));
    }
    else {
        return compareLeaf(tree, compare);
    }
};
export const compareTrees = (tree, compare) => compareTree(tree.tree, compare.tree);
//# sourceMappingURL=SwapTreeCompare.js.map